#include <bits/stdc++.h>
using namespace std;

#define ppi pair<pair<int, int>, int>

int main(){
	// map<int, int> map;
	// //them phan tu bang key, value se ghi de duoc value
	// map[1] = 100;
	// map.insert({2, 300});
	
	// //duyet map bang for each
	// for(pair<int, int> x: map){
	// 	cout << x.first << " " << x.second << endl;
	// }
	// //iterator
	// for (auto it = map.begin(); it != map.end(); ++it){
	// 	cout << (*it).first << " " << (*it).second << endl;
	// }

//1 so ham: emty, find, count, erase

//dem so lan xuat hien theo thu tu xuat hien
	// int n; cin >> n;
	// int a[n];
	// map<int, int> map;
	// for (int i=0; i<n; ++i){
	// 	cin >> a[i];
	// 	map[a[i]]++;
	// }

	// for (int x : a){
	// 	if (map[x] > 0){
	// 		cout << x << " " << map[x] << endl;
	// 		map[x] = 0;
	// 	}
	// }

	int n; cin >> n; int k; cin >> k;
	for(int i=0; i<n; ++i){
		cin >> a[i]
	}
}